name: Deploy

on:
  push:
    branches: ["main"]
  workflow_dispatch:
    inputs:
      ref:
        description: "Commit SHA to deploy (previous release SHA = rollback)"
        required: false
        type: string

permissions:
  contents: read

concurrency:
  group: deploy-production
  cancel-in-progress: false

jobs:
  check:
    uses: ./.github/workflows/check.yml
    with:
      ref: ${{ inputs.ref || github.sha }}

  deploy:
    needs: check
    runs-on: ubuntu-latest
    environment: production
    # Host/user via env, not spliced into run: scripts — removes the shell-injection
    # surface and keeps values off the command line (GitHub Actions hardening guidance).
    env:
      DEPLOY_HOST: ${{ secrets.DEPLOY_HOST }}
      DEPLOY_USER: ${{ secrets.DEPLOY_USER }}
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ inputs.ref || github.sha }}

      - name: Record release SHA
        run: echo "${{ inputs.ref || github.sha }}" > RELEASE

      - name: Join tailnet (ephemeral tag:ci node)
        uses: tailscale/github-action@v4
        with:
          oauth-client-id: ${{ secrets.TS_OAUTH_CLIENT_ID }}
          oauth-secret: ${{ secrets.TS_OAUTH_SECRET }}
          tags: tag:ci

      - name: Wait for CT reachability
        # Validate the ACTUAL deploy path — a Tailscale-SSH connection — with a
        # warmup retry. Plain `tailscale ping` is wrong here: the NAT'd CT and the
        # NAT'd ephemeral runner can only meet over a DERP relay, and `tailscale
        # ping` exits non-zero unless a DIRECT path forms (it never will). DERP is
        # fine for SSH/rsync; this loop proves that end-to-end while giving the
        # tag:ci node time to learn the peer + establish the DERP path.
        run: |
          for i in $(seq 1 12); do
            if ssh -o StrictHostKeyChecking=accept-new -o ConnectTimeout=8 \
                 "$DEPLOY_USER@$DEPLOY_HOST" true 2>/dev/null; then
              echo "CT reachable via Tailscale SSH (attempt $i)"; exit 0
            fi
            echo "waiting for CT SSH reachability ($i/12)…"; sleep 5
          done
          echo "CT did not become SSH-reachable within ~60s" >&2; exit 1

      - name: Rsync source to CT
        run: |
          rsync -az --delete \
            --exclude ".git" --exclude ".venv" --exclude "staticfiles" \
            -e "ssh -o StrictHostKeyChecking=accept-new" \
            ./ "$DEPLOY_USER@$DEPLOY_HOST:/opt/hw-radar/app/"

      - name: Migrate and restart (venv built on-CT)
        run: |
          ssh -o StrictHostKeyChecking=accept-new \
            "$DEPLOY_USER@$DEPLOY_HOST" \
            "bash /opt/hw-radar/app/deploy/deploy-remote.sh"

      - name: Smoke test
        # Type=exec units return from `restart` once gunicorn execs, not once it is
        # listening — poll healthz with a bounded retry so a slow worker boot doesn't
        # trip a false deploy failure (and needless rollback).
        run: |
          ssh -o StrictHostKeyChecking=accept-new "$DEPLOY_USER@$DEPLOY_HOST" '
            for _ in $(seq 1 15); do
              if curl -fsS http://127.0.0.1:8000/healthz; then exit 0; fi
              sleep 2
            done
            echo "healthz did not become ready within ~30s" >&2
            exit 1
          '

      - name: Static smoke test (through CT nginx)
        # /healthz goes straight to gunicorn and never exercises nginx static
        # serving, which is how bug 001 (403 on /static/) shipped green. Fetch a
        # collected Django admin asset through the CT nginx instead. The Host
        # header is read from the rsynced nginx conf (its server_name) rather than
        # repeated here; without it the request lands on nginx's default site.
        # tests/unit/test_static_root_contract.py pins the conf shape and asset.
        run: |
          ssh -o StrictHostKeyChecking=accept-new "$DEPLOY_USER@$DEPLOY_HOST" '
            conf=/opt/hw-radar/app/deploy/nginx/hw-radar.conf
            host=$(awk "\$1 == \"server_name\" { sub(/;\$/, \"\", \$2); print \$2; exit }" "$conf")
            if [ -z "$host" ]; then echo "no server_name in $conf" >&2; exit 1; fi
            asset=/static/admin/css/base.css
            code=000
            for _ in $(seq 1 5); do
              code=$(curl -sS -o /dev/null -w "%{http_code}" -H "Host: $host" "http://127.0.0.1$asset") || code=000
              if [ "$code" = 200 ]; then echo "static OK: $asset"; exit 0; fi
              sleep 2
            done
            echo "nginx returned HTTP $code for $asset (expected 200)" >&2
            exit 1
          '
